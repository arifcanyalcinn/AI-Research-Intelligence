# Technical Debt

Tracked debt across all batches. Each item records why it was accepted and
what would remove it.

| ID | Issue | Why Accepted | Future Action |
|----|-------|--------------|---------------|
| TD-001 | respx 0.21.1 bug #277 — `mock.get()` never matches (bytes/str method mismatch) | Pinned version; blocks all source HTTP tests | Upgrade to respx >= 0.22.0, replace `url__startswith=` with `mock.get()`, delete the notice in `tests/unit/sources/conftest.py` |
| TD-002 | 13 ruff `W292` / `W293` errors across 7 files: `arip/__init__.py`, the three `huggingface_*.py` sources, and their three test files | Pre-existing from Batch 3; unrelated to later batches | `ruff check --fix` in a standalone cleanup commit |
| TD-003 | `abstract` is always `None` for HF Models and HF Spaces | List endpoints return no description; fetching the model/space card costs one extra HTTP request per item | Optional enrichment pass if generation quality proves insufficient |
| TD-004 | Model `title` is the repo ID (`org/name`), not a human-readable name | The models list endpoint exposes no display name | Resolve from the model card alongside TD-003 |
| TD-005 | HF Models / Spaces fetch limit and sort key are hardcoded constants | See D-005 — SDS types both as plain `SourceConfig` | Requires an SDS amendment to become configurable |
| TD-006 | `ruff format` is not enforced; 14 files would be reformatted | Project gate is `ruff check` only (D-004 in the older numbering) | Decide whether to adopt `ruff format` project-wide, then reformat in one commit |
| TD-007 | `github_trending` reads `ARIP_GITHUB_TOKEN` from `os.environ`, bypassing `AppSettings` | Required by D-006 because the existing registry contract cannot carry top-level secrets | Revisit if a future SDS revision allows plugins to receive application-level secrets |
| TD-008 | `.env.example` cites a 60 req/hour GitHub limit; this refers to the core API, while the Search API has different limits | Documentation-only inaccuracy; unrelated to Batch 5 implementation | Correct in a separate housekeeping commit |
| TD-009 | `papers_with_code` is declared in the SDS but unimplemented; only 5 of 6 sources are active | The current Papers With Code service publishes no documented public read API (see PROJECT_LOG Batch 6) | Implement Batch 6 when an official API is published; SDS §8 L1494 and L1509 gates cannot be met until then |
| TD-010 | All five source plugins hardcode `language="EN"` in `normalize()`, while SDS §5.4 requires the initial language to be assigned "based on config" | The plugin construction contract passes only the per-source `SourceConfig` (see D-006 rationale), which does not carry `pipeline.languages`. Separately, `pipeline.languages` is a `list[str]` while `NormalizedItem.language` is a single `str` — the SDS defines no mapping between them | Resolve when multi-language support is implemented (SDS §9.2 defers multi-language-per-run to Phase 7). Requires either an SDS amendment defining the list→single mapping, or a change to the plugin construction contract |
| TD-011 | SDS §4.2 lists a `raw_payload` column on the `items` table; SDS §4.7 states the `items` table "no longer carries a `raw_payload` column". The two sections contradict each other | The implementation correctly follows §4.7: `items` has no such column, and raw payloads live in `raw_source_payloads`. **Do not "fix" this by adding the column back** — §4.2's row is the stale text. Verified: `Item(..., raw_payload='{}')` raises `TypeError` | Remove the `raw_payload` row from the §4.2 column table in a future SDS revision. Until then this entry exists to stop a future reader reconciling the contradiction backwards |
| TD-012 | `raw_source_payloads.fetch_url` is `NULL` for every row the pipeline writes, so a stored payload does not record which URL produced it | The `RawSourcePayload` entity in `arip/entities.py` carries no URL field. Adding one changes Batch 1 infrastructure and every source's `fetch()`, which is outside Batch 7 scope. The column is nullable, so the schema permits it. Consequence: the normalization-replay capability described in SDS §4.7 works, but cannot attribute a payload to its originating request | Add a `fetch_url` field to the `RawSourcePayload` entity and populate it in each source's `fetch()`, in the batch that implements normalization replay or the next batch that already modifies all five sources |
| TD-013 | `NormalizedItem` field types do not mirror `items` column types: `published_date` is an ISO `str` but the column is `Date`; five fields are Python containers but the columns are JSON `TEXT`; `raw_payload` has no column at all | `NormalizedItem` is a transport object shared by all five sources, and nothing wrote it to the database before Batch 7. `CollectStage._item_columns()` is the single translation point and handles all three cases. Verified: passing the ISO string straight to the `Date` column raises `StatementError` | Anyone adding a field to `NormalizedItem` must update `_item_columns()` in the same change. Consider aligning the two shapes if the field set grows. Related: TD-011 |
| TD-014 | ARIP assumes runtime directories and schema that nothing creates on a fresh checkout. `alembic upgrade head` fails because `data/` does not exist and SQLite will not create a parent directory; `arip run` against an unmigrated database fails with a bare `no such table: pipeline_runs` and a full traceback | Correct per SDS §4.1 and `database.py::init_db` — production setup is `alembic upgrade head`, and the SDS specifies no bootstrap step. `container.py::_ensure_runtime_dirs` does create `data/` and `logs/`, but it runs inside `build_app_components()`, which is after the migration in the documented setup order | Either document a bootstrap step (`mkdir data logs` before `alembic upgrade head`), or add a startup check that reports missing directories and un-applied migrations with an actionable message. Requires deciding whether the CLI may create directories or run migrations — neither is specified by the SDS |
| TD-015 | Two concurrent `arip run` processes corrupt run bookkeeping: the second calls `reconcile_crashed()` and marks the first run's still-legitimate RUNNING row as FAILED, after which the first completes and marks its own FAILED row COMPLETED | AD-01 specifies one process and `schedule` is single-threaded, so this cannot happen on the intended path. Nothing prevents an operator running `arip run` manually while a scheduled run is in progress. Item data is unaffected — deduplication still holds — but the `pipeline_runs` history becomes misleading | Add a process lock, or have `reconcile_crashed()` only reconcile rows older than a threshold, in the batch that implements the §5.1 scheduler |

## Removed Debt

None yet.